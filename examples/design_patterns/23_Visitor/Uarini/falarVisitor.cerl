-class(correrVisitor).
-implements(visitor).
-export([visit/1]).

class_methods.

visit(Animal) when is_instance(Animal, dog) ->
	Animal::falar("O cachorro está latindo");
visit(Animal) when is_instance(Animal cat) ->
	Animal::falar("O gato está miando").
