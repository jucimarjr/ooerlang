-class(template).
-export([main/0]).

class_methods.

main() ->
	Terremoto = relatorioTerremoto::constructor(),
	Umidade = relatorioUmidade::constructor(),

	io:format(Terremoto::montarRelatorio()),
	io:format(Umidade::montarRelatorio()).
