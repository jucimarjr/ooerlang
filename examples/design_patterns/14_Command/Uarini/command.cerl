-class(command).
-export([main/0]).

main() ->
	Soma = somar::constructor(),
	Subtrair = subtrair::constructor(),

	io:format("~p~n", [Soma::operar()]),
	io:format("~p~n", [Subtrair::operar()]).
