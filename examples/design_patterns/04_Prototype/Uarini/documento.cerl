-abstract(documento).
-implements(cloneable).

class_methods.

protected clone() ->
	Clone = object::null(),
	try
		Clone = Super::clone();
	catch
		error:Error -> {error, caught, Error}
	end
	(Documento) clone.

